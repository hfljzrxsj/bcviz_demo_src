import type { Options } from "ahooks/lib/useRequest/src/types";
//: Options<any, Array<any>>
export const commonUseRequestParams = Object.freeze({
  loadingDelay: 300,
  throttleWait: 300,
  debounceWait: 300,
  debounceLeading: true,
});